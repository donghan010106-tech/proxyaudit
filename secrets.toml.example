# Copy this file to .streamlit/secrets.toml and fill in your own key
# to enable LLM-phrased chat answers in the "Ask the auditor" tab.
# Without this, the app still works fully using the built-in template fallback.

ANTHROPIC_API_KEY = "sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxx"
